import clsx from 'clsx';
import { memo } from 'react';
import style from './style.module.css';
const RegisterPage = () => {
    return (
        <>
            <div className={clsx(style.content)}>RegisterPage</div>
        </>
    );
};

export default memo(RegisterPage);
